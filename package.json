{
  "name": "chatapp",
  "version": "0.0.0",
  "description": "chat app based on Code School's redis chat app & socket.io's example from github",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Michael Erwin",
  "license": "ISC",
  "dependencies": {
    "express": "^4.11.1",
    "socket.io": "^1.3.2"
  }
}
