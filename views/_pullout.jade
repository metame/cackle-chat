.container_pullout
  .container_users
    .div_users
      h3 <span class="users-count"></span> <span class="users-word">users</span> in #{room}
      ul.list_users
        if users[0]
          each user in users
            li= user
        else
          li.empty no one here yet!
  .container_rooms
    .div_rooms
      h3 Chat Rooms
      .div_room Room 1
      .div_room Room 2
      .div_room Room 3